# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20

# Document handling
pillow==11.1.0          # image handling + demo document rendering
pypdf==5.1.0            # embedded PDF text extraction

# Optional real OCR. Requires the tesseract binary on the machine:
#   Ubuntu/Debian : sudo apt install tesseract-ocr
#   macOS         : brew install tesseract
#   Windows       : https://github.com/UB-Mannheim/tesseract/wiki
# Without it, the app still runs: PrintedOCR/HandwritingOCR fall back to a
# clearly-labelled demo mode with realistic recognition noise and confidence.
pytesseract==0.3.13
